# Code generated by pangea-forge.
# InSpec profile for verifying datadog_cloud_inventory_sync_config infrastructure state.
#
# Usage:
#   1. Create the resource using Pangea or Terraform
#   2. Set DATADOG_TOKEN and DATADOG_GATEWAY_URL environment variables
#   3. Run: inspec exec profiles/datadog_cloud_inventory_sync_config/ -t datadog://

control 'datadog_cloud_inventory_sync_config-exists' do
  impact 1.0
  title 'Manages a datadog_cloud_inventory_sync_config resource'
  desc 'Verify that the datadog_cloud_inventory_sync_config resource exists and has expected properties'

  describe datadog_cloud_inventory_sync_config(name: attribute('id')) do
    it { should exist }
    its('cloud_provider') { should_not be_nil }
  end
end
